Hi,
This is just to let you know that <%= @reservation.manager.first_name%> <%= @reservation.manager.last_name%> just approved your reservation for the kitchen from <%=@reservation.start_at.strftime("%a, %b %d, %I:%M %p")%> to <%=@reservation.end_at.strftime("%a, %b %d, %I:%M %p")%>, and will be in touch with you shortly.
-The kitchen app
P.S. Replying to this email is probably futile : )
